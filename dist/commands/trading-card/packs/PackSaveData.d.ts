export declare class PackSaveData {
    id: number;
    name: string;
    stars: number;
    possibleItemsIds: Array<number>;
    constructor(id: number, name: string, stars: number, possibleItemsIds: Array<number>);
}
